# Create a farm
farm = Farm()

# Add some animals
farm.add_animal(Cow(5, 100, 500))
farm.add_animal(Chicken(2, 100, 2))

# Simulate 10 days
for day in range(1, 11):
    print(f"\nDay {day}:")
    farm.simulate_day()
    print(f"Remaining food: {farm.food}")
